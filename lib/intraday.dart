import 'package:flutter/material.dart';

class intraday extends StatelessWidget {
  const intraday({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Intraday", style: TextStyle(fontSize: 30),),
      ),
    );
  }
}
